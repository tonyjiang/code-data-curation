[project]
name = "code-data-curation"
version = "0.1.0"
description = "Data curation workflows"
requires-python = ">=3.10,<3.14"
dependencies = [
    "flytekit",
    "huggingface-hub>=0.30,<1",
    "pyarrow>=15,<20",
    "tree-sitter-language-pack>=1.18,<2",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "pytest>=8,<9",
]
